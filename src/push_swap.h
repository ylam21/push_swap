#ifndef PUSH_SWAP_H
#define PUSH_SWAP_H

typedef struct s_list {
	void *content;
	struct s_list *next;
}	t_list;

// Allowed
// #include <unistd.h>
// Delete later
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

#endif
